import { StyleSheet } from 'react-native';
import { WIDTH, HEIGHT, FONT, SIZE } from './static';
export default StyleSheet.create({
    helpTxt: {
        textAlign: 'right',
        fontFamily: FONT.regular,
        fontWeight: 'normal',
        margin:7,
        fontSize:16,
       lineHeight:35
      },
})


