import { StyleSheet } from 'react-native';
import { HEIGHT, WIDTH } from '../../styles/static';

export default StyleSheet.create({
  container: {
    // justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'white',
    height: HEIGHT,
    width: WIDTH,
  },
});
